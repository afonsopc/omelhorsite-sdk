{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": []
  },
  "include": [
    "src/**/*.ts",
    "scripts/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ],
  "//": "`scripts` is included so the isolate checker is typechecked under the same strict settings as the code it guards. It deliberately uses no host API - only the TypeScript compiler API and `import.meta.url` - so it survives this project's poisoned `process`."
}
